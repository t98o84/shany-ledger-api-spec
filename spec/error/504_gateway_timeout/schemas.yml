GatewayTimeoutError:
  title: GatewayTimeoutError
  description: 未認証の場合のエラー詳細です。
  allOf:
  - $ref: "../schemas.yml#/ProblemDetails"
  - properties:
      status:
        type: integer
        enum:
        - 401
      code:
        type: string
        enum:
        - UnauthorizedError
