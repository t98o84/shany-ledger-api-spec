WorkspaceUnitGroupsSearchSuccessResponse:
  description: 台帳の管理単位のグループ一覧の取得が成功した場合のレスポンスです。
  content:
    application/json:
      schema:
        $ref: "./schemas.yml#/WorkspaceUnitGroups"

WorkspaceUnitGroupFindSuccessResponse:
  description: 台帳の管理単位のグループの取得が成功した場合のレスポンスです。
  content:
    application/json:
      schema:
        $ref: "./schemas.yml#/WorkspaceUnitGroup"

WorkspaceUnitGroupCreateSuccessResponse:
  description: 台帳の管理単位のグループの取得が成功した場合のレスポンスです。
  content:
    application/json:
      schema:
        $ref: "./schemas.yml#/WorkspaceUnitGroup"

WorkspaceUnitGroupUpdateSuccessResponse:
  description: 台帳の管理単位のグループの更新が成功した場合のレスポンスです。

WorkspaceUnitGroupDeleteSuccessResponse:
  description: 台帳の管理単位のグループの削除が成功した場合のレスポンスです。
